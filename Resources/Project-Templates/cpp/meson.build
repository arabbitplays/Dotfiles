project('{{PROJECT_NAME}}', 'cpp',
  version : '0.1.0',
  default_options : ['warning_level=3', 'cpp_std=c++23']
)

sources = files(
  'src/main.cpp',
)

executable('{{PROJECT_NAME}}',
  sources,
  install : true,
)
